source 'https://rubygems.org'
gem 'minitest'
gem 'rake'
gem 'dotenv'
gem 'reline' # Added to silence warnings
gem 'irb'    # Added to silence warnings
gem 'logger' # Added to silence warnings

group :development, :test do
  gem 'pry' # Added pry back as it is a dependency for pry-byebug
  gem 'pry-byebug'
  gem 'simplecov'
end
